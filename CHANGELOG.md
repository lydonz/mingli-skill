# Changelog

## 2.0.0 — 2026-08-06

MingLi Skill 2.0.0 是一次不保留长期兼容性的重大重构，重点提升排盘一致性、可审计性、普通用户体验和安全边界。

### 主要变化

- 引入共享 `ComputedChart`，统一八字、紫微、大运、流年和流月使用的命盘与时间语义。
- 修复午夜 `hour=0` 错盘，并将 `year_boundary`、`zi_hour_convention` 贯穿相关计算。
- 增加时区、真太阳时、夏令时异常和出生时间误差候选命盘处理。
- 实现流月分段；旺衰升级为 `seasonal-strength-v2`，喜忌升级为 `element-balance-v1`。
- 紫微精确后端失败时不再输出近似命盘，避免把降级结果误当成精确结果。
- 退役历史事件选择题预测与旧 Benchmark 专用入口。
- 重构规则引擎，减少重复逻辑并统一解释证据。
- 六爻保留原始摇币结果并支持随机种子复现。
- 收紧奇门、面相、手相、风水等功能的适用范围与安全提示。
- 加入本地知识包、SHA-256 完整性校验、解释契约和 HTML 报告。
- 增加持续集成、项目伦理说明、方法论说明、Python 打包配置和回归测试。

### 破坏性变更

- Python 最低版本提高到 3.11。
- Node.js 最低版本为 20，紫微运行时固定使用 `iztro==2.5.8`。
- 删除旧 Benchmark、个人样本、机器绑定脚本和历史事件预测入口。
- 删除紫微近似回退；精确后端不可用时会明确失败。
- 部分输出新增版本、证据、不确定性和安全字段，调用方应按新结构适配。

### 固定依赖

- `lunar-python==1.4.8`
- `pyarrow==21.0.0`
- `geonamescache==2.0.0`
- `pypinyin==0.55.0`
- `iztro==2.5.8`
